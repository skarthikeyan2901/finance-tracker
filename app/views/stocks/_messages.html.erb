<div class="mt-2">
  <div class="card card-header">
    <div>
      <%= message.content %>
      <strong><%= message.user.full_name %></strong>
    </div>
    <div>
      <%= time_ago_in_words(message.created_at) %>
    </div>
  </div>
</div>